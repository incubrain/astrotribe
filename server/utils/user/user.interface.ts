import type { SelectInput, GenericReturn } from '../base.interface'
import { User } from './user.model'

export interface IUserRepository {
  // selectUserCards(config: SelectInput<{}>): GenericReturn<User>
}
