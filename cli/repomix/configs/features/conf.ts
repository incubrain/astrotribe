import type { OutputConfig } from '../../types'

export const FEATURE_SETTINGS = {
  output: {
    directory: 'ai-context',
    filename: '',
    extension: 'txt' as OutputConfig['extension'],
  } as OutputConfig,
}
