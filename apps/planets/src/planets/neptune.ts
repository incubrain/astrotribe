import type { PlanetConfig } from '../utils/constants'
import { PLANETS } from '../utils/constants'

export const neptuneConfig: PlanetConfig = PLANETS.neptune

// Any Neptune-specific logic can be added here if needed in the future
