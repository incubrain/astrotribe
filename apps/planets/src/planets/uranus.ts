import type { PlanetConfig } from '../utils/constants'
import { PLANETS } from '../utils/constants'

export const uranusConfig: PlanetConfig = PLANETS.uranus

// Any Uranus-specific logic can be added here if needed in the future
