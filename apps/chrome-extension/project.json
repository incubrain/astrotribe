{
  "name": "@astronera/chrome-extension",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/chrome-extension",
  "targets": {
    "build:clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist/apps/chrome-extension"
      }
    },
    "build:html": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mkdir -p dist/apps/chrome-extension && cp apps/chrome-extension/newtab.html dist/apps/chrome-extension/"
      }
    },
    "build:assets": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cp apps/chrome-extension/manifest.json dist/apps/chrome-extension/"
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/chrome-extension",
        "target": "chrome"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      },
      "dependsOn": ["build:clean", "build:html", "build:assets"]
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "chrome-extension:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "chrome-extension:build:development"
        },
        "production": {
          "buildTarget": "chrome-extension:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/chrome-extension/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"**/*.{js,ts,jsx,tsx,json,md,yml,yaml}\" --config ../../prettier.config.mjs"
      }
    },
    "format:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --check \"**/*.{js,ts,jsx,tsx,json,md,yml,yaml}\" --config ../../prettier.config.mjs"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd dist/apps/chrome-extension && zip -r ../chrome-extension.zip ."
      },
      "dependsOn": ["build"]
    }
  },
  "tags": ["type:app", "scope:chrome-extension"]
}
