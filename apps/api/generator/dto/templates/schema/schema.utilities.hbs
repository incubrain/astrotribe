/** * Schema utilities for
{{name}}
*/

{{#each fields}}
  {{#if validationRules}}
    /** * Zod schema for
    {{name}}
    field */ export const
    {{name}}Schema = z.{{zodType}}{{#unless isRequired}}.optional(){{/unless}};

  {{/if}}
{{/each}}