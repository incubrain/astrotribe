// types/plan.types.ejs
export enum SubscriptionStatus {
  CREATED = 'CREATED',
  AUTHENTICATED = 'AUTHENTICATED',
  CHARGED = 'CHARGED',
  COMPLETED = 'COMPLETED',
  EXPIRED='EXPIRED',
  CANCELED='CANCELED',
}
