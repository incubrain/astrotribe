{
  "name": "admin-dashboard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/admin-dashboard",
  "// targets": "to see all targets run: nx show project admin-dashboard --web",
  "targets": {
    "build:base": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd layers/base && npx nuxi build"
      }
    },
    "build:auth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd layers/auth && npx nuxi build"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/admin-dashboard && npx nuxi build"
      },
      "dependsOn": ["build:base", "build:auth"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --config eslint.config.js ."
      }
    }
  }
}
